import ClientHeader from "@/components/ClientHeader";
import ClientsList from "@/components/ClientsList";

const page = () => {
  return (
    <div>
      <ClientHeader />
      <ClientsList />
    </div>
  );
};

export default page;
